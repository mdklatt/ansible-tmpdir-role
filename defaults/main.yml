## Default variables for the tmpdir role.
##
## These variables have the lowest priority and can be overriden at runtime.
##
tmpdir_root: "{{ ansible_env.TMPDIR|default('/tmp') }}"
tmpdir_template: tmp.XXXXXXXX
tmpdir_debug: False
